package client.sync;

/**
 * Stub: Resolves conflicts between local and remote document versions.
 */
public class ConflictResolver {
    public ConflictPair resolve(ConflictPair pair) {
        // Stub — will implement conflict resolution logic later
        return pair;
    }
}
